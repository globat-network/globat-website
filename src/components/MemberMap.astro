---
import { getMembers, getMemberCountsByCountry } from "@/data/members";
import MapRenderer from "@/components/MapRenderer.tsx";

const members = await getMembers();
const countries = getMemberCountsByCountry(members);
---

<div class="block leading-none">
  <MapRenderer client:only counts={countries} />
</div>
