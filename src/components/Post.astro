---
import { formatDate } from "@/lib/util";

const { post } = Astro.props;
---

<li>
  <a
    href={`/news/${post.id}`}
    class="link flex w-full flex-col gap-1 sm:flex-row sm:items-center sm:justify-between sm:gap-4"
  >
    <span>{post.data.title}</span>
    <div
      class="text-muted-text dark:text-dark-muted-text shrink-0 text-sm font-normal decoration-0"
    >
      {formatDate(post.data.publicationDate)}
    </div>
  </a>
</li>
